"use client";

export default function TrackerFooter() {
  return (
    <footer className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
      <p className="max-w-xl font-serif text-[28px] leading-tight sm:text-[30px]">
        &ldquo;Success is the Product of Daily Habits&rdquo;
        <span className="mt-1 block text-[14px] font-semibold text-zinc-700">- James Clear</span>
      </p>
    </footer>
  );
}
